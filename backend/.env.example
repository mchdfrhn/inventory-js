# Environment Configuration
NODE_ENV=development

# Server Configuration
PORT=8080
HOST=localhost

# Database Configuration
DB_DRIVER=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=123
DB_NAME=inventaris
DB_SSL=false

# Migration URL (for node-pg-migrate)
DATABASE_URL=postgres://postgres:123@localhost:5432/inventaris

# MySQL specific (if using MySQL)
DB_CHARSET=utf8mb4
DB_TIMEZONE=+07:00

# ==============================================
# BACKEND ENVIRONMENT CONFIGURATION
# ==============================================
# Salin file ini menjadi .env dan sesuaikan dengan konfigurasi Anda

# Environment Configuration
# development | production | test
NODE_ENV=development

# Server Configuration
# Port untuk menjalankan server backend (default: 8080)
PORT=8080
# Host server (gunakan 0.0.0.0 untuk production)
HOST=localhost

# Database Configuration
# ==============================================
# Pilih salah satu database yang akan digunakan:

# POSTGRESQL (Default - Recommended)
# Jenis database yang digunakan
DB_DIALECT=postgres
# Host database PostgreSQL (biasanya localhost untuk development)
DB_HOST=localhost
# Port database PostgreSQL (default: 5432)
DB_PORT=5432
# Username database PostgreSQL
DB_USER=postgres
# Password database PostgreSQL (GANTI dengan password yang aman!)
DB_PASSWORD=123
# Nama database yang akan digunakan
DB_NAME=inventaris
# SSL untuk koneksi database (false untuk development, true untuk production)
DB_SSL=false

# ==============================================
# CONTOH KONFIGURASI DATABASE LAIN:
# ==============================================

# MYSQL / MariaDB
# Uncomment dan sesuaikan jika menggunakan MySQL/MariaDB
# DB_DIALECT=mysql
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=yourpassword
# DB_NAME=inventaris
# DB_SSL=false
# DB_CHARSET=utf8mb4
# DB_TIMEZONE=+07:00

# SQLITE (untuk development/testing)
# Uncomment jika menggunakan SQLite
# DB_DIALECT=sqlite
# DB_STORAGE=./database.sqlite
# # Note: Untuk SQLite, hanya perlu DB_DIALECT dan DB_STORAGE

# MICROSOFT SQL SERVER
# Uncomment dan sesuaikan jika menggunakan SQL Server
# DB_DIALECT=mssql
# DB_HOST=localhost
# DB_PORT=1433
# DB_USER=sa
# DB_PASSWORD=yourpassword
# DB_NAME=inventaris
# DB_SSL=false
# DB_ENCRYPT=true
# DB_TRUST_SERVER_CERTIFICATE=true

# ==============================================
# CONNECTION URL (Alternative Configuration)
# ==============================================
# Anda juga bisa menggunakan connection URL sebagai alternatif:

# PostgreSQL URL
# DATABASE_URL=postgres://username:password@host:port/database

# MySQL URL  
# DATABASE_URL=mysql://username:password@host:port/database

# SQLite URL
# DATABASE_URL=sqlite:./database.sqlite

# SQL Server URL
# DATABASE_URL=mssql://username:password@host:port/database

# JWT Configuration
# Secret key untuk JWT token (WAJIB DIGANTI dengan string yang unik dan aman!)
# Gunakan generator online atau: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production
# Durasi berlaku token (format: 1h, 24h, 7d, dll)
JWT_EXPIRES_IN=24h

# Logging Configuration
# Level log: error | warn | info | debug
LOG_LEVEL=info
# Path file log relatif dari root project
LOG_FILE=logs/app.log

# Rate Limiting (Pembatasan Request)
# Durasi window dalam milidetik (900000 = 15 menit)
RATE_LIMIT_WINDOW_MS=900000
# Maximum request per window per IP
RATE_LIMIT_MAX=100

# File Upload Configuration
# Ukuran maksimum file dalam bytes (5242880 = 5MB)
MAX_FILE_SIZE=5242880
# Folder penyimpanan file upload
UPLOAD_PATH=uploads/

# CORS Configuration
# Domain frontend yang diizinkan akses (gunakan domain production untuk production)
CORS_ORIGIN=http://localhost:5173

# Application Information
# Informasi aplikasi untuk keperluan logging dan monitoring
APP_NAME=STTPU Inventory Management System
APP_VERSION=1.0.0
APP_DEVELOPER=Mochammad Farhan Ali
APP_ORGANIZATION=STTPU

# Logging
LOG_LEVEL=info
LOG_FILE=logs/app.log

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_PATH=uploads/

# CORS
CORS_ORIGIN=http://localhost:5173

# Application Info
APP_NAME=STTPU Inventory Management System
APP_VERSION=1.0.0
APP_DEVELOPER=Mochammad Farhan Ali
APP_ORGANIZATION=STTPU
